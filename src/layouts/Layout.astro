---
// 1. ESPAÇO DE LÓGICA (JAVASCRIPT) - Fica entre os "---"
interface Props {
	title: string;
}

import Footer from '../components/Footer.astro';

const { title } = Astro.props;
---

<!doctype html>
<html lang="pt-BR">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Imobiliária Profissional" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body>
		<slot /> <Footer />
	</body>
</html>

<style is:global>
  /* 2. ESPAÇO DE ESTILO (CSS) - Fica dentro da tag <style> */
  @import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;600;700&display=swap');

  /* =========================================
     RESET GLOBAL (A VACINA ANTI-QUEBRA DE TELA)
  ========================================= */
  *, *::before, *::after {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  html, body {
    max-width: 100vw;
    overflow-x: hidden; /* Corta tudo que vazar para a direita! */
  }

  /* Garante que imagens nunca quebrem a tela */
  img {
    max-width: 100%;
    height: auto;
  }

  /* =========================================
     VARIÁVEIS E BASE
  ========================================= */
  :root {
    --bg-creme: #fdfdfd;
    --stone-100: #f1f5f9;
    --primary: #0f172a;
    --accent: #2563eb;
    --text-main: #1e293b;
    --text-muted: #64748b;
  }

  body {
    font-family: 'Plus Jakarta Sans', sans-serif;
    background-color: var(--bg-creme);
    /* Fundo Complexo: Gradiente radial + textura sutil */
    background-image: 
      radial-gradient(at 0% 0%, rgba(37, 99, 235, 0.03) 0px, transparent 50%),
      radial-gradient(at 100% 100%, rgba(15, 23, 42, 0.02) 0px, transparent 50%);
    min-height: 100vh;
    color: var(--text-main);
  }

  /* Adiciona uma granulação fina no fundo para parecer papel de qualidade */
  body::before {
    content: "";
    position: fixed;
    top: 0; left: 0; width: 100%; height: 100%;
    opacity: 0.2;
    pointer-events: none;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E");
    z-index: -1;
  }

  h1, h2, h3 { 
    color: var(--primary); 
    font-weight: 700; 
    line-height: 1.2;
  }

  .section-container {
    max-width: 1200px;
    width: 100%; /* Garante que respeite a largura da tela */
    margin: 0 auto;
    padding: 60px 20px;
  }

  /* Ajuste automático dos containers para o celular */
  @media (max-width: 768px) {
    .section-container {
      padding: 40px 16px; /* Reduz margens no mobile */
    }
  }
</style>